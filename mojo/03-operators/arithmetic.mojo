def main():
    var a: Int = 17
    var b: Int = 5

    print("a + b =", a + b)
    print("a - b =", a - b)
    print("a * b =", a * b)
    print("a / b =", Float64(a) / Float64(b))
    print("a // b =", a // b)
    print("a % b =", a % b)
    print("a ** b =", a ** b)
    print("-a =", -a)
