FROM debian:bookworm-slim

RUN apt-get update && \
    apt-get install -y --no-install-recommends algol68g && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

WORKDIR /app

ENTRYPOINT ["a68g"]
